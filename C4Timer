/* Plugin generated by AMXX-Studio */

#include <amxmodx>
#include <cstrike>

#define PLUGIN 	"C4 Timer and Sound"
#define VERSION "1.6"
#define AUTHOR 	"kwpd"

new g_c4timer
new cvar_showteam
new cvar_flash
new cvar_sprite
new cvar_msg
new g_Cvar
new g_C4
new g_msg_showtimer
new g_msg_roundtime
new g_msg_scenario
new pcvarToggle

#define MAX_SPRITES	2
#define task_sound 69696969

new const g_timersprite[MAX_SPRITES][] = { "bombticking", "bombticking1" }
new const g_message[] = "Detonation time intiallized....."

public plugin_init() 
{
	register_plugin(PLUGIN, VERSION, AUTHOR)
	
	cvar_showteam	= register_cvar("amx_showc4timer", "3")
	cvar_flash	= register_cvar("amx_showc4flash", "0")
	cvar_sprite	= register_cvar("amx_showc4sprite", "1")
	cvar_msg	= register_cvar("amx_showc4msg", "0")
	
	pcvarToggle = register_cvar("C4_voice", "2")
	
	g_msg_showtimer	= get_user_msgid("ShowTimer")
	g_msg_roundtime	= get_user_msgid("RoundTime")
	g_msg_scenario	= get_user_msgid("Scenario")
	
	register_event("HLTV", "event_hltv", "a", "1=0", "2=0")
	register_logevent("RoundEnd",2,"1=Round_End")
	register_logevent("RoundStart", 2, "1=Round_Start")
	register_logevent("logevent_plantedthebomb", 3, "2=Planted_The_Bomb")
	
	g_C4	= get_cvar_pointer("mp_c4timer")
	g_Cvar	= register_cvar("amx_soundc4", "1")
}

public plugin_precache() 
{
	precache_sound("C4timer/ten.wav")
	precache_sound("C4timer/nine.wav")
	precache_sound("C4timer/eight.wav")
	precache_sound("C4timer/seven.wav")
	precache_sound("C4timer/six.wav")
	precache_sound("C4timer/five.wav")
	precache_sound("C4timer/four.wav")
	precache_sound("C4timer/three.wav")
	precache_sound("C4timer/two.wav")
	precache_sound("C4timer/one.wav")
	
	precache_sound("C4timer/1.wav")
	precache_sound("C4timer/2.wav")
	precache_sound("C4timer/3.wav")
	precache_sound("C4timer/4.wav")
	precache_sound("C4timer/5.wav")
	precache_sound("C4timer/6.wav")
	precache_sound("C4timer/7.wav")
	precache_sound("C4timer/8.wav")
	precache_sound("C4timer/9.wav")
	precache_sound("C4timer/10.wav")
	
	return PLUGIN_HANDLED
} 

public event_hltv()
{
	g_c4timer = get_pcvar_num(g_C4)
}

public logevent_plantedthebomb()
{
	new showtteam = get_pcvar_num(cvar_showteam)
	
	static players[32], num, i
	switch(showtteam)
	{
		case 1: 
		{
			get_players(players, num, "ace", "TERRORIST")
		}
		case 2: 
		{
			get_players(players, num, "ace", "CT")
		}
		case 3: 
		{
			get_players(players, num, "ac")
		}
		default: return
	}
	for(i = 0; i < num; ++i) set_task(1.0, "update_timer", players[i])
}

public update_timer(id)
{
	message_begin(MSG_ONE_UNRELIABLE, g_msg_showtimer, _, id)
	message_end()
	
	message_begin(MSG_ONE_UNRELIABLE, g_msg_roundtime, _, id)
	write_short(g_c4timer)
	message_end()
	
	message_begin(MSG_ONE_UNRELIABLE, g_msg_scenario, _, id)
	write_byte(1)
	write_string(g_timersprite[clamp(get_pcvar_num(cvar_sprite), 0, (MAX_SPRITES - 1))])
	write_byte(150)
	write_short(get_pcvar_num(cvar_flash) ? 20 : 0)
	message_end()
	
	if(get_pcvar_num(cvar_msg))
	{
		set_hudmessage(255, 180, 0, 0.44, 0.87, 2, 6.0, 6.0)
		show_hudmessage(id, g_message)
	}
}

public bomb_planted(planter) 
{
	new Name[32]
	new id = task_sound
	
	if(get_pcvar_num(g_Cvar))
	{
		get_user_name(planter, Name, 31)  
		
		new time = get_pcvar_num(g_C4)
		
		float(time)
		set_task( (time - 10.0) , "Zero", task_sound+id)
		set_task( (time - 9.0) , "one", task_sound+id)
		set_task( (time - 8.0) , "two", task_sound+id)
		set_task( (time - 7.0) , "three", task_sound+id)
		set_task( (time - 6.0) , "foor", task_sound+id)
		set_task( (time - 5.0) , "five", task_sound+id)
		set_task( (time - 4.0) , "six", task_sound+id)
		set_task( (time - 3.0) , "seven", task_sound+id)
		set_task( (time - 2.0) , "eigth", task_sound+id)
		set_task( (time - 1.0) , "nine", task_sound+id)
	}
	return PLUGIN_CONTINUE  
}

public Zero()  
{
	if((get_pcvar_num(pcvarToggle) == 1))
	{
		client_cmd(0, "spk C4timer/ten.wav")
	}
	if((get_pcvar_num(pcvarToggle) == 2))
	{
		client_cmd(0, "spk C4timer/10.wav" )
	}
	return PLUGIN_CONTINUE
}

public one()  
{
	if((get_pcvar_num(pcvarToggle) == 1))
	{
		client_cmd(0, "spk C4timer/nine.wav")
	}
	if((get_pcvar_num(pcvarToggle) == 2))
	{
		client_cmd(0, "spk C4timer/9.wav" )
	}
	return PLUGIN_CONTINUE
}

public two()  
{
	if((get_pcvar_num(pcvarToggle) == 1))
	{
		client_cmd(0, "spk C4timer/eight.wav")
	}
	if((get_pcvar_num(pcvarToggle) == 2))
	{
		client_cmd(0, "spk C4timer/8.wav" )
	}
	return PLUGIN_CONTINUE
}

public three()  
{
	if((get_pcvar_num(pcvarToggle) == 1))
	{
		client_cmd(0, "spk C4timer/seven.wav")
	}
	if((get_pcvar_num(pcvarToggle) == 2))
	{
		client_cmd(0, "spk C4timer/7.wav" )
	}
	return PLUGIN_CONTINUE
}

public foor()  
{ 
	if((get_pcvar_num(pcvarToggle) == 1))
	{
		client_cmd(0, "spk C4timer/six.wav")
	}
	if((get_pcvar_num(pcvarToggle) == 2))
	{
		client_cmd(0, "spk C4timer/6.wav" )
	}
	return PLUGIN_CONTINUE
}

public five()  
{
	if((get_pcvar_num(pcvarToggle) == 1))
	{
		client_cmd(0, "spk C4timer/five.wav")
	}
	if((get_pcvar_num(pcvarToggle) == 2))
	{
		client_cmd(0, "spk C4timer/5.wav" )
	}
	return PLUGIN_CONTINUE
}

public six()  
{
	if((get_pcvar_num(pcvarToggle) == 1))
	{
		client_cmd(0, "spk C4timer/four.wav")
	}
	if((get_pcvar_num(pcvarToggle) == 2))
	{
		client_cmd(0, "spk C4timer/4.wav" )
	}
	return PLUGIN_CONTINUE
}

public seven()  
{
	if((get_pcvar_num(pcvarToggle) == 1))
	{
		client_cmd(0, "spk C4timer/three.wav")
	}
	if((get_pcvar_num(pcvarToggle) == 2))
	{
		client_cmd(0, "spk C4timer/3.wav" )
	}
	return PLUGIN_CONTINUE
}

public eigth()  
{
	if((get_pcvar_num(pcvarToggle) == 1))
	{
		client_cmd(0, "spk C4timer/two.wav")
	}
	if((get_pcvar_num(pcvarToggle) == 2))
	{
		client_cmd(0, "spk C4timer/2.wav" )
	}
	return PLUGIN_CONTINUE
}

public nine()  
{
	if((get_pcvar_num(pcvarToggle) == 1))
	{
		client_cmd(0, "spk C4timer/one.wav")
	}
	if((get_pcvar_num(pcvarToggle) == 2))
	{
		client_cmd(0, "spk C4timer/1.wav" )
	}
	return PLUGIN_CONTINUE
}

public RoundEnd()
{
	new id = task_sound
	remove_task(task_sound+id)
}

public RoundStart()  
{
	new id = task_sound
	remove_task(task_sound+id)
}
